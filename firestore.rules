rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 개발 중에는 누구나 데이터를 읽고 쓸 수 있도록 허용합니다.
      allow read, write: if true;
    }
  }
}