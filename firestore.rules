rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 누구나 데이터를 읽을 수 있도록 허용하지만, 쓰기는 로그인한 사용자만 가능합니다.
      allow read;
      allow write: if request.auth != null;
    }
  }
}